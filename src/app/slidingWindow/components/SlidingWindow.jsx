import React from 'react';
import Gist from 'react-gist';

const SlidingWindow = () => (
  <div>
    <p>
      Write a function called <b>maxSubArraySum</b> which accepts an array of
      integers and n. The function should calculate the maximum sum of
      concecutive n elements in array.
    </p>
    <Gist id="b1dd67912ac5b9e9aea9e36185f48612" />
  </div>
);

export default SlidingWindow;
